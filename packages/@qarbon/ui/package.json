{
  "name": "@qarbon/ui",
  "version": "0.1.0",
  "description": "QarbonQuery package",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix",
    "check-types": "tsc --noEmit",
    "test": "jest",
    "test:watch": "jest --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {}
}
